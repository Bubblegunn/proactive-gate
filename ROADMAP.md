# Roadmap

Three next steps, in no particular order. Open an issue if you want to take one.

- A `PostgresStore` alongside `MemoryStore` and `RedisStore`
- A `digest` helper that batches rejected candidates into one later delivery
- An adapter package for Vercel AI SDK middleware

Things deliberately not planned: anything that needs a server, a hosted account, or reads code content.
